# pull all binary variables to the first stage and generate Benders cuts using Callback

# master problem that has all binary variables
function pullMaster(pData,disData,Ω)
end

# sub problem that is a linear program
function pullSub(pData,disData,Ω,ω)
end
